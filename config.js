// Websocket Server Address (DNS). "127.0.0.1" for local development.
var websocket_server_addr = "127.0.0.1";

// Websocket Server Port
var websocket_server_port = 8080;

// Websocket Protocol ("ws"/"wss")
var websocket_server_protocol = "ws";

// Display connection info in the upper left corner. (true/false)
var show_connection_infos = true;